### issue 1

When I try to commit using commit button the request failed says request failed. teh actual flow should be when I click commit button it should commit to github to `{username}/README.md`

issues

[POST] `http://localhost:8080/publish/readme` 404 Not Found
response: {"error":"Not Found"}

### issue 2

I wnat on commit button I want to show a modal so user can give commit message if not it sends by default update readme else user gien commit and commits to github
